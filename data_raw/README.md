# Input data

Third-party input data are not bundled. See `docs/data_sources.md` and
`config/study.json` for source resources, relative paths and acquisition details.
Use `--source-root` to read an existing data directory without copying it here.
